---

- name: Install docker
  apt:
    name: docker.io
    state: present
    update_cache: yes
  
- name: Ensure docker group exists
  group:
    name: docker
    state: present

- name: Add current ansible user to docker group
  user:
    name: "{{ ansible_user }}"
    groups: docker
    append: true